services:
  db:
    image: postgres:16-alpine
    container_name: dnews-africa-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dnews_africa
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dnews_africa"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
